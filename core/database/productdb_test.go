package database

import (
	"testing"
	. "github.com/franela/goblin"
)

const ConfigFile = "./../../config.json"

func TestAuthModel(t *testing.T) {
	g := Goblin(t)




	//g.Describe("tags", func() {
	//	g.It("should be split into an array", func() {
	//		g.Assert(filterTags("tag, technology, something, another")).IsTrue()
	//	})
	//})
}

